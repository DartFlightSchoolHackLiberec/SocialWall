
library socialwall.models.feed.googleplusfeed;

import 'dart:core';
import 'Feed.dart';
import 'Post.dart';

class GooglePlusFeed extends Feed
{
  GooglePlusFeed(configuration) : super(configuration);
}
